services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      MONGO_URI: "mongodb+srv://mohitashliya:iKkqhno4bnCXdNDM@cluster0.b7q8e.mongodb.net/?appName=Cluster0"

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      BACKEND_URL: "http://backend:5000/submit"
    depends_on:
      - backend